import {StyleSheet} from 'react-native';

export default () =>
  StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#ccc',
      padding: 10,
    },
    text: {
      fontSize: 18,
      fontWeight: 'bold',
      color: '#f0f',
    },
  });
